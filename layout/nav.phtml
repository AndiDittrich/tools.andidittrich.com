<ul class="nav nav-tabs" id="primary-nav">
    <?php

        foreach ($pages as $page => $navTitle){
            $active = ($page == $pageName ? ' class="active"' : '');

            echo '<li role="presentation"', $active, '><a href="', $page, '.html">', $navTitle, '</a></li>';
        }

    ?>
</ul>